{
  "name": "@signposts/site",
  "private": true,
  "type": "module",
  "//": "The Signposts website (signposts.asim.dev). Standalone Astro project — NOT a workspace of the repo root (the root is the publishable signposts CLI package). Driven via `just site-dev` / `just site-build`.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.2",
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/fraunces": "^5.2.9",
    "@fontsource-variable/inter": "^5.2.8",
    "@fontsource/ibm-plex-mono": "^5.2.7",
    "@fontsource/press-start-2p": "^5.2.7",
    "astro": "^7.0.6",
    "yaml": "^2.9.0"
  }
}
